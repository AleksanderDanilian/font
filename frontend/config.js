// Конфигурация фронтенда. Вынесено отдельно от app.js, чтобы менять base URL
// бэкенда без правки логики (см. раздел 11 ТЗ — конфигурация не хардкодится
// внутри кода). Если фронт раздаётся с того же origin, что и API (как
// теперь — main.py монтирует frontend/ сам на себя) — оставляем пустую
// строку, запросы идут по относительным путям и работают на любом порту.
// Если фронт всё же раздаётся отдельным сервером на другом порту/хосте —
// пропишите здесь полный адрес бэкенда, например "http://localhost:8010".
window.APP_CONFIG = {
  API_BASE: "",
};
